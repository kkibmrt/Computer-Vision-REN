# Video Processing pada Computer Vision
## Pengolahan Video Realtime Menggunakan OpenCV dan Python

---

# Apa Itu Video Processing?

Video Processing adalah proses pengolahan video frame demi frame menggunakan algoritma Computer Vision.

Pada Computer Vision, video sebenarnya merupakan kumpulan gambar yang ditampilkan secara cepat sehingga terlihat bergerak.

Video processing digunakan untuk:
- object detection realtime
- motion tracking
- AI surveillance
- robotics vision
- autonomous system

---

# Fungsi Video Processing

Beberapa fungsi utama video processing:

| Fungsi | Penjelasan |
|---|---|
| Realtime Monitoring | Pemantauan realtime |
| Motion Detection | Deteksi pergerakan |
| Object Tracking | Pelacakan objek |
| AI Surveillance | Sistem keamanan AI |
| Robotics Vision | Penglihatan robot |
| Autonomous Navigation | Navigasi otomatis |

---

# Workflow Video Processing

```text
Camera Input
      ↓
Frame Acquisition
      ↓
Image Processing
      ↓
Realtime Analysis
      ↓
Display Result
```

---

# Membuka Kamera

Langkah pertama pada video processing adalah membuka kamera.

```python
import cv2


camera = cv2.VideoCapture(0)
```

---

# Penjelasan `VideoCapture()`

```python
cv2.VideoCapture()
```

digunakan untuk:
- menghubungkan program dengan webcam
- membaca video
- mengambil frame realtime

---

# Parameter Kamera

| Parameter | Fungsi |
|---|---|
| 0 | Kamera utama/default |
| 1 | Kamera eksternal |
| 2 | Kamera tambahan |

---

# Mengecek Status Kamera

```python
import cv2


camera = cv2.VideoCapture(0)


if camera.isOpened():
    print("Camera successfully opened")

else:
    print("Could not open camera")
```

---

# Penjelasan `isOpened()`

```python
camera.isOpened()
```

digunakan untuk memeriksa:
- kamera aktif
- kamera gagal dibuka

---

# Penyebab Kamera Gagal

Beberapa penyebab:
- kamera dipakai aplikasi lain
- driver kamera bermasalah
- permission belum diberikan
- webcam tidak terhubung

---

# Membaca Frame Kamera

```python
import cv2


camera = cv2.VideoCapture(0)


while True:

    success, frame = camera.read()

    if not success:
        print("Failed to read frame")
        break


    cv2.imshow('Realtime Video', frame)


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan Program

## `camera.read()`

Digunakan untuk membaca frame terbaru dari kamera.

```python
success, frame = camera.read()
```

---

# Output `read()`

| Variabel | Fungsi |
|---|---|
| success | Status pembacaan frame |
| frame | Gambar/frame video |

---

# Penjelasan `imshow()`

```python
cv2.imshow()
```

digunakan untuk:
- menampilkan frame video
- menampilkan hasil processing

---

# Keyboard Control

```python
if cv2.waitKey(1) == ord('q'):
    break
```

Program berhenti jika tombol:
```text
q
```
ditekan.

---

# Menutup Kamera

```python
camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan

## `release()`

Digunakan untuk:
- melepaskan kamera
- menghentikan akses webcam

## `destroyAllWindows()`

Digunakan untuk:
- menutup semua window OpenCV

---

# Membaca Properti Kamera

```python
import cv2


camera = cv2.VideoCapture(0)


width = int(camera.get(cv2.CAP_PROP_FRAME_WIDTH))

height = int(camera.get(cv2.CAP_PROP_FRAME_HEIGHT))

fps = int(camera.get(cv2.CAP_PROP_FPS))


print("Width :", width)
print("Height:", height)
print("FPS:", fps)
```

---

# Camera Properties

| Property | Fungsi |
|---|---|
| CAP_PROP_FRAME_WIDTH | Lebar frame |
| CAP_PROP_FRAME_HEIGHT | Tinggi frame |
| CAP_PROP_FPS | Frame per second |

---

# Apa Itu FPS?

FPS (Frame Per Second) adalah jumlah frame yang ditampilkan setiap detik.

Contoh:
- 30 FPS → 30 frame/detik
- 60 FPS → 60 frame/detik

Semakin tinggi FPS:
- video lebih halus
- respon AI lebih cepat

---

# Video Realtime Grayscale

```python
import cv2


camera = cv2.VideoCapture(0)


while True:

    success, frame = camera.read()

    if not success:
        break


    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)


    cv2.imshow('Gray Video', gray)


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan Grayscale Video

Setiap frame video:
- diubah menjadi grayscale
- diproses realtime
- ditampilkan kembali

---

# Edge Detection Realtime

```python
import cv2


camera = cv2.VideoCapture(0)


while True:

    success, frame = camera.read()

    if not success:
        break


    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)

    edges = cv2.Canny(gray, 100, 200)


    cv2.imshow('Edge Detection', edges)


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan Canny Realtime

Program:
- membaca frame kamera
- mengubah ke grayscale
- melakukan edge detection
- menampilkan hasil realtime

---

# Realtime Color Detection

```python
import cv2
import numpy as np


camera = cv2.VideoCapture(0)


while True:

    success, frame = camera.read()

    if not success:
        break


    hsv = cv2.cvtColor(frame, cv2.COLOR_BGR2HSV)


    lower_blue = np.array([100,150,0])

    upper_blue = np.array([140,255,255])


    mask = cv2.inRange(hsv, lower_blue, upper_blue)


    result = cv2.bitwise_and(frame, frame, mask=mask)


    cv2.imshow('Original', frame)

    cv2.imshow('Mask', mask)

    cv2.imshow('Result', result)


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan Color Detection

Program:
- membaca kamera realtime
- mengubah ke HSV
- mendeteksi warna biru
- menampilkan hasil filtering

---

# Menyimpan Video

OpenCV dapat menyimpan hasil video processing.

```python
import cv2


camera = cv2.VideoCapture(0)


fourcc = cv2.VideoWriter_fourcc(*'XVID')


out = cv2.VideoWriter(
    'output.avi',
    fourcc,
    20.0,
    (640,480)
)


while True:

    success, frame = camera.read()

    if not success:
        break


    out.write(frame)


    cv2.imshow('Recording', frame)


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

out.release()

cv2.destroyAllWindows()
```

---

#  Penjelasan VideoWriter

```python
cv2.VideoWriter()
```

digunakan untuk:
- merekam video
- menyimpan hasil AI vision
- export hasil processing

---

# Parameter VideoWriter

| Parameter | Fungsi |
|---|---|
| output.avi | Nama file |
| fourcc | Format codec |
| 20.0 | FPS video |
| (640,480) | Resolusi video |

---

# Workflow AI Video Vision

```text
Camera Realtime
      ↓
Frame Acquisition
      ↓
Preprocessing
      ↓
AI Detection
      ↓
Object Tracking
      ↓
Realtime Display
```

---


# Teknologi yang Digunakan

| Teknologi | Fungsi |
|---|---|
| Python | Bahasa pemrograman |
| OpenCV | Pengolahan video |
| NumPy | Operasi array |
| Webcam | Input realtime |
| Computer Vision | Analisis visual |

---
