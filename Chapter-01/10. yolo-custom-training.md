# Custom YOLO Training pada Computer Vision
## Training Dataset Sendiri Menggunakan YOLO dan Python

---

# Apa Itu Custom YOLO Training?

Custom YOLO Training adalah proses melatih model YOLO menggunakan dataset buatan sendiri agar AI dapat mendeteksi objek tertentu sesuai kebutuhan project.

Dengan custom training, YOLO dapat mendeteksi:
- objek khusus
- benda buatan sendiri
- komponen robot
- kapal
- alat industri
- object project AI

---

# Fungsi Custom Training

Custom YOLO digunakan pada berbagai project ex:

| Bidang | Contoh Penggunaan |
|---|---|
| Robotics | Deteksi obstacle robot |
| Autonomous Ship | Deteksi kapal dan buoy |

---

# Workflow Custom YOLO Training

```text
Collect Dataset
        ↓
Image Labeling
        ↓
Dataset Annotation
        ↓
Dataset Split
        ↓
YOLO Training
        ↓
Model Validation
        ↓
Realtime Detection
```

---

# Tahapan Custom YOLO Training

Tahapan utama:
- mengambil dataset
- labeling object
- training model
- testing model
- deployment realtime

---

# Dataset pada YOLO

Dataset adalah kumpulan gambar yang digunakan AI untuk belajar mengenali objek.

Contoh dataset:
- kapal
- manusia
- robot
- kendaraan
- alat industri

---

# Cara Mengambil Dataset

Dataset dapat diperoleh dari:
- kamera HP
- webcam
- CCTV
- drone
- video recording

---

# Mengambil Dataset dari Video

```python
import cv2


camera = cv2.VideoCapture('video.mp4')


count = 0


while True:

    success, frame = camera.read()

    if not success:
        break


    cv2.imwrite(
        f'dataset/frame_{count}.jpg',
        frame
    )


    count += 1


camera.release()


print("Dataset collection finished")
```

---

# Penjelasan Program

Program:
- membaca video
- mengambil frame video
- menyimpan gambar dataset

---

# Struktur Dataset

```text
dataset/
    frame_0.jpg
    frame_1.jpg
    frame_2.jpg
```

---

# Apa Itu Labeling?

Labeling adalah proses:
- memberi nama objek
- membuat bounding box
- menentukan class object

---

# Tools Labeling Dataset

Beberapa tools labeling populer:

| Tools | Fungsi |
|---|---|
| LabelImg | Labeling manual |
| Roboflow | Dataset management |
| CVAT | Labeling professional |
| Makesense.ai | Labeling online |

---

# Format Label YOLO

YOLO menggunakan format:

```text
class_id center_x center_y width height
```

---

# Contoh Label YOLO

```text
0 0.512 0.421 0.231 0.312
```

---

# Penjelasan Format Label

| Nilai | Fungsi |
|---|---|
| 0 | ID class |
| center_x | Titik tengah horizontal |
| center_y | Titik tengah vertical |
| width | Lebar object |
| height | Tinggi object |

---

# Struktur Dataset YOLO

```text
dataset/
    images/
        train/
        valid/

    labels/
        train/
        valid/
```

---

# Penjelasan Struktur Dataset

| Folder | Fungsi |
|---|---|
| images | Menyimpan gambar |
| labels | Menyimpan annotation |
| train | Dataset training |
| valid | Dataset validasi |

---

# Dataset Split

Dataset biasanya dibagi:

| Jenis | Persentase |
|---|---|
| Training | 70% |
| Validation | 20% |
| Testing | 10% |

---

# Install Ultralytics YOLO

YOLO modern menggunakan library:

```bash
pip install ultralytics
```

---

# Import YOLO

```python
from ultralytics import YOLO
```

---

# Download YOLO Model

```python
model = YOLO('yolov8n.pt')
```

---

# Penjelasan YOLOv8

YOLOv8 adalah versi modern YOLO yang:
- lebih cepat
- lebih akurat
- lebih mudah digunakan

---

# Jenis YOLOv8 Model

| Model | Keterangan |
|---|---|
| yolov8n | Nano |
| yolov8s | Small |
| yolov8m | Medium |
| yolov8l | Large |
| yolov8x | Extra Large |

---

# File Dataset YAML

YOLO memerlukan file dataset YAML.

Contoh:

```yaml
train: dataset/images/train
val: dataset/images/valid

nc: 1

names:
  - ship
```

---

# Penjelasan YAML

| Parameter | Fungsi |
|---|---|
| train | Path dataset training |
| val | Path validation |
| nc | Jumlah class |
| names | Nama class |

---

# Training YOLO

```python
from ultralytics import YOLO


model = YOLO('yolov8n.pt')


model.train(
    data='dataset.yaml',
    epochs=50,
    imgsz=640
)
```

---

# Penjelasan Training

Program:
- load model YOLO
- membaca dataset
- melakukan training AI
- menyimpan hasil training

---

# Parameter Training

| Parameter | Fungsi |
|---|---|
| data | File dataset YAML |
| epochs | Jumlah iterasi training |
| imgsz | Ukuran gambar training |

---

# Apa Itu Epoch?

Epoch adalah jumlah pengulangan AI membaca seluruh dataset.

Contoh:
- 50 epochs
- 100 epochs
- 300 epochs

Semakin besar epoch:
- AI semakin belajar
- training lebih lama

---

# Hasil Training

Setelah training selesai:

```text
runs/detect/train/
```

akan berisi:
- weights
- grafik training
- hasil validasi

---

# File Hasil Training

```text
best.pt
last.pt
```

---

# Penjelasan Weight Model

| File | Fungsi |
|---|---|
| best.pt | Model terbaik |
| last.pt | Model terakhir |

---

# Testing Model YOLO

```python
from ultralytics import YOLO


model = YOLO('best.pt')


results = model('test.jpg')


results[0].show()
```

---

# Penjelasan Testing

Program:
- load hasil training
- melakukan detection
- menampilkan hasil object detection

---

# YOLO Realtime Detection

```python
from ultralytics import YOLO
import cv2


model = YOLO('best.pt')


camera = cv2.VideoCapture(0)


while True:

    success, frame = camera.read()

    if not success:
        break


    results = model(frame)


    annotated_frame = results[0].plot()


    cv2.imshow(
        'YOLO Realtime',
        annotated_frame
    )


    if cv2.waitKey(1) == ord('q'):
        break


camera.release()

cv2.destroyAllWindows()
```

---

# Penjelasan Realtime Detection

Program:
- membuka kamera realtime
- menjalankan YOLO
- mendeteksi objek custom
- menampilkan bounding box realtime

---

# GPU pada Training YOLO

Training YOLO lebih cepat menggunakan GPU.

Contoh GPU:
- NVIDIA RTX
- Jetson Xavier
- Jetson Nano
- RTX Laptop GPU

---

# CPU vs GPU Training

| Hardware | Keterangan |
|---|---|
| CPU | Training lambat |
| GPU | Training sangat cepat |

---

# Penggunaan Custom YOLO

Custom YOLO digunakan pada:
- autonomous robot
- kapal tanpa awak
- smart surveillance
- industrial AI
- robotics competition
- AI monitoring system

---

# Workflow AI Detection System

```text
Dataset Collection
        ↓
Labeling Dataset
        ↓
YOLO Training
        ↓
Realtime Detection
        ↓
Tracking System
        ↓
AI Decision Making
```
| Ultralytics | Framework YOLO |
| GPU | Accelerasi training |
| Computer Vision | Analisis visual |
