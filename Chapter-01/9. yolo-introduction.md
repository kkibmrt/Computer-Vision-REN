# YOLO Introduction pada Computer Vision
## Pengenalan YOLO Object Detection Menggunakan Python

---

# Apa Itu YOLO?

YOLO adalah singkatan dari:

```text
You Only Look Once
```

YOLO merupakan algoritma Object Detection berbasis Deep Learning yang mampu:
- mendeteksi objek
- mengenali jenis objek
- menentukan posisi objek
- bekerja secara realtime

YOLO menjadi salah satu metode Object Detection paling populer karena:
- sangat cepat
- akurat
- cocok untuk realtime AI vision

---

# Fungsi YOLO

YOLO digunakan pada berbagai bidang teknologi modern:

| Bidang | Contoh Penggunaan |
|---|---|
| Autonomous Vehicle | Deteksi kendaraan |
| Robotics | Robot vision |
| Smart Surveillance | CCTV AI |
| Industrial Automation | Quality control |
| Smart City | Traffic monitoring |
| Military AI | Target detection |

---

# Perbedaan YOLO dan Traditional Detection

| Metode | Cara Kerja |
|---|---|
| Haar Cascade | Feature manual |
| Contour Detection | Bentuk objek |
| YOLO | Deep Learning |

---

# Keunggulan YOLO

| Keunggulan | Penjelasan |
|---|---|
| Realtime | Sangat cepat |
| Akurat | Deteksi lebih stabil |
| Multi Object | Bisa mendeteksi banyak objek |
| Deep Learning | Lebih modern |
| Scalable | Bisa custom dataset |

---

# Workflow YOLO Detection

```text
Camera / Image Input
          ↓
Image Preprocessing
          ↓
CNN Deep Learning
          ↓
Feature Extraction
          ↓
Object Detection
          ↓
Bounding Box
          ↓
Class Prediction
          ↓
Realtime Output
```

---

# Cara Kerja YOLO

YOLO membagi gambar menjadi beberapa grid.

Setiap grid bertugas untuk:
- mendeteksi objek
- menentukan bounding box
- menentukan probabilitas objek

---

# Konsep Bounding Box

Bounding box terdiri dari:

```text
x
y
width
height
```

---

# Konsep Confidence Score

Confidence score menunjukkan:
- tingkat keyakinan AI terhadap objek

Contoh:

```text
Person : 95%
Car    : 88%
Bottle : 76%
```

---

# Install Library YOLO

Install OpenCV:

```bash
pip install opencv-python
```

Install NumPy:

```bash
pip install numpy
```

---

# Import Library

```python
import cv2
import numpy as np
```

---

# File yang Dibutuhkan YOLO

YOLO memerlukan:
- weight
- config
- class names

---

# Struktur File YOLO

```text
yolo/
    yolov3.weights
    yolov3.cfg
    coco.names
```

---

# Penjelasan File YOLO

| File | Fungsi |
|---|---|
| .weights | Bobot hasil training |
| .cfg | Arsitektur model |
| .names | Nama class objek |

---

# Load YOLO Model

```python
import cv2


net = cv2.dnn.readNet(
    'yolov3.weights',
    'yolov3.cfg'
)
```

---

# Penjelasan `readNet()`

```python
cv2.dnn.readNet()
```

digunakan untuk:
- membaca model YOLO
- load deep learning network

---

# Load Nama Class

```python
classes = []


with open('coco.names', 'r') as f:

    classes = [
        line.strip()
        for line in f.readlines()
    ]
```

---

# Penjelasan Class Names

Class names digunakan untuk:
- menampilkan nama objek
- menentukan label detection

Contoh class:
- person
- car
- bicycle
- dog
- bottle

---

# Membaca Gambar

```python
image = cv2.imread('image.jpg')
```

---

# Mengetahui Ukuran Gambar

```python
height, width, channels = image.shape
```

---

# Membuat Blob

YOLO menggunakan blob sebagai input model.

```python
blob = cv2.dnn.blobFromImage(
    image,
    1/255,
    (416,416),
    (0,0,0),
    swapRB=True,
    crop=False
)
```

---

# Penjelasan Blob

Blob digunakan untuk:
- preprocessing image
- normalisasi pixel
- menyesuaikan ukuran input AI

---

# Parameter Blob

| Parameter | Fungsi |
|---|---|
| image | Input gambar |
| 1/255 | Normalisasi pixel |
| (416,416) | Ukuran input |
| swapRB | Konversi warna |
| crop | Crop image |

---

# Mengirim Blob ke YOLO

```python
net.setInput(blob)
```

---

# Mendapatkan Output Layer

```python
layer_names = net.getLayerNames()


output_layers = [
    layer_names[i - 1]
    for i in net.getUnconnectedOutLayers()
]
```

---

# Melakukan Object Detection

```python
outputs = net.forward(output_layers)
```

---

# Penjelasan Forward

```python
net.forward()
```

digunakan untuk:
- menjalankan inference AI
- melakukan object detection

---

# Full YOLO Detection Program

```python
import cv2
import numpy as np


net = cv2.dnn.readNet(
    'yolov3.weights',
    'yolov3.cfg'
)


classes = []


with open('coco.names', 'r') as f:

    classes = [
        line.strip()
        for line in f.readlines()
    ]


layer_names = net.getLayerNames()


output_layers = [
    layer_names[i - 1]
    for i in net.getUnconnectedOutLayers()
]


image = cv2.imread('image.jpg')


height, width, channels = image.shape


blob = cv2.dnn.blobFromImage(
    image,
    1/255,
    (416,416),
    (0,0,0),
    swapRB=True,
    crop=False
)


net.setInput(blob)


outputs = net.forward(output_layers)


boxes = []

confidences = []

class_ids = []


for output in outputs:

    for detection in output:

        scores = detection[5:]

        class_id = np.argmax(scores)

        confidence = scores[class_id]


        if confidence > 0.5:

            center_x = int(detection[0] * width)

            center_y = int(detection[1] * height)

            w = int(detection[2] * width)

            h = int(detection[3] * height)


            x = int(center_x - w / 2)

            y = int(center_y - h / 2)


            boxes.append([x,y,w,h])

            confidences.append(float(confidence))

            class_ids.append(class_id)


indexes = cv2.dnn.NMSBoxes(
    boxes,
    confidences,
    0.5,
    0.4
)


for i in range(len(boxes)):

    if i in indexes:

        x,y,w,h = boxes[i]

        label = str(classes[class_ids[i]])


        cv2.rectangle(
            image,
            (x,y),
            (x+w,y+h),
            (0,255,0),
            2
        )


        cv2.putText(
            image,
            label,
            (x,y-10),
            cv2.FONT_HERSHEY_SIMPLEX,
            0.5,
            (0,255,0),
            2
        )


cv2.imshow('YOLO Detection', image)

cv2.waitKey(0)

cv2.destroyAllWindows()
```

---

# Penjelasan NMS

```python
cv2.dnn.NMSBoxes()
```

NMS adalah:

```text
Non Maximum Suppression
```

digunakan untuk:
- menghapus bounding box duplicate
- memilih detection terbaik

---

# Realtime YOLO Detection

```python
camera = cv2.VideoCapture(0)
```

YOLO juga dapat digunakan secara realtime:
- webcam
- CCTV
- drone camera
- robotics camera

---

# Workflow YOLO Realtime

```text
Camera Input
      ↓
Frame Processing
      ↓
YOLO Inference
      ↓
Bounding Box
      ↓
Object Classification
      ↓
Realtime Display
```

---

# Kelebihan YOLO

| Kelebihan | Penjelasan |
|---|---|
| Cepat | Cocok realtime |
| Akurat | Detection stabil |
| Multi Object | Banyak objek sekaligus |
| Deep Learning | Modern AI |
| GPU Support | Performa tinggi |

---

# Kekurangan YOLO

| Kekurangan | Penjelasan |
|---|---|
| Butuh GPU | Untuk performa maksimal |
| Model besar | Memori lebih tinggi |
| Training lama | Dataset besar |

---

# Penggunaan YOLO pada AI

YOLO digunakan pada:
- autonomous vehicle
- robotics vision
- AI surveillance
- drone AI
- smart traffic
- industrial automation

---

# Workflow AI Vision Modern

```text
Camera Realtime
      ↓
Deep Learning Model
      ↓
Object Detection
      ↓
Tracking System
      ↓
Decision Making
      ↓
Autonomous Action
```

---
| NumPy | Operasi array |
| GPU | Accelerasi AI |
| Computer Vision | Analisis visual |
